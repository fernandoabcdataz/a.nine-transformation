version: 2

sources:
  - name: raw
    config:
      enabled: true
    schema: "{{ var('client') }}_ingestion"
    tables:
      - name: xero_accounts
      - name: xero_bank_transactions
      - name: xero_invoices
      - name: xero_payments